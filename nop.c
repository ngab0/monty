#include "monty.h"

/**
 * nop -  does nothing
 * @stack: doesnt matter
 * @count : for nothing
 *
 * Return: NOTHING
 */
void nop(stack_t **stack, unsigned int count)
{
	(void) stack;
	(void) count;
}
